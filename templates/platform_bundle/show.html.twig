{% extends 'base.html.twig' %}

{% block title %}PlatformBundle{% endblock %}

{% block body %}
    <h1>PlatformBundle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ platform_bundle.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('platform_bundle_index') }}">back to list</a>

    <a href="{{ path('platform_bundle_edit', {'id': platform_bundle.id}) }}">edit</a>

    {{ include('platform_bundle/_delete_form.html.twig') }}
{% endblock %}
